data/
.DS_Store

# v2: documento en CLARO producido por tools/csv_to_json.py — NUNCA subir.
# El único artefacto que se versiona es data.enc (cifrado por el navegador).
data.json

# Notas y utilidades locales (no forman parte de la app)
.claude/
__pycache__/
CONTEXT.md
HANDOFF.md
NEXT_STEPS.md

# ── Nada personal entra aquí ─────────────────────────────────────────────
# El repositorio es PÚBLICO. Los extractos del banco, los ficheros de trabajo
# de la reconstrucción y cualquier export en claro se quedan fuera: lo único
# tuyo que puede vivir aquí es data.enc, que va cifrado.
BANKINTER_DATOS/
SANTANDER_DATOS/
MYINVESTOR_DATOS/
REVOLUT_DATOS/
TRADEREPUBLIC_DATOS/
ABIES_DATOS/
Solvento_EXPORT/
Homebrew_TTRPG/
*EN-CLARO*.json
solvento-datos-*.json
reglas.tsv
apuntes.tsv
clasificacion.tsv
migracion.tsv
posiciones.tsv
reclasificaciones.tsv
_prueba-*.html
_datos-prueba.json

# Las copias del bloque cifrado viven fuera del repositorio; por si alguien
# apunta el destino aquí dentro, que no puedan subirse igualmente.
Solvento_COPIAS/
copias/
